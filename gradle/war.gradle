apply plugin: "war"

bootWar {
    mainClassName = "com.pingsec.dev.UtilsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
